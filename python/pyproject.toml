[project]
name = "interactive-transcripts-speech"
version = "0.1.0"
description = "Local speech transcription server for Interactive Transcripts"
requires-python = ">=3.10"
dependencies = [
    "parakeet-mlx>=0.5.2",
]

[dependency-groups]
dev = []
